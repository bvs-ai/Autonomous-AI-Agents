# Базові залежності демок лекції 2 (усе, крім 016 і 018 -- див. requirements_langgraph.txt).
# Версії -- ті, на яких демо перевірені (липень 2026).

# Валідація вхід/вихід інструментів: 002-006, 012, 015, 019.
pydantic>=2.11,<3

# Клієнт OpenAI-сумісного API: 006, 007.
openai>=2.41,<3

# Тайм-аути й мок мережі для тестів: 008, 009, 012.
httpx>=0.27,<1

# Retry з backoff+jitter: 008, 009, 012.
tenacity>=9.0,<10

# @tool / args_schema без повного LangChain: 015.
langchain-core>=1.0,<2

# Читання .env (OPENAI_BASE_URL / OPENAI_API_KEY / LLM_MODEL): 006, 007, 016, 018.
python-dotenv>=1.0

# Тести: 012.
pytest>=8.0
