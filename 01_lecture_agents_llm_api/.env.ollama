# Конфігурація для локальної моделі через Ollama.
# Використання:  cp .env.ollama .env   (або: set -a; source .env.ollama; set +a)
# Перед цим: ollama serve && ollama pull qwen3  (див. 015_ollama_setup.sh)

OPENAI_BASE_URL=http://localhost:11434/v1
OPENAI_API_KEY=ollama
LLM_MODEL=qwen2.5:7b
